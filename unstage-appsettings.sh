#At this step I also removed the appsettings.json from git staging + gitignored it
git rm --cached ./server/api/appsettings.json

#In the git history it appears as a deleted file, but its just ignored. 